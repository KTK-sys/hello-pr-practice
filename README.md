# Hello PR Practice

A tiny smaple project for practicing the GitHub pull request workflow: fork/clone, branch, commit, and open a PR.

## What's here

- `greet.py` — a small script that prints a greeting.

## Usage

```bash
python greet.py
```

## Running tests

```bash
python -m unittest discover -s tests
```
